// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  botName: "helpBOT",
  initialMessages: [createChatBotMessage(`Hello Esteemed Customer`)]
}

export default config
